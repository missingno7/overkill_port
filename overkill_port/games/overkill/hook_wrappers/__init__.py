"""OVERKILL hook registration modules.

These modules keep address-bound @registry.replace wrappers close to the game
subsystem they adapt, while :mod:`overkill_port.replacements` remains the
backward-compatible import surface that registers all hooks.
"""
