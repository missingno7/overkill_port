# Campaign: FRONT-END (tier: SCREEN-exact)

**Scope.** Title/options → level-select (grid nav + fire recovered; cursor render 5A00/5A6C open) →
cold-start the chosen level; game-over returns to title (Spine's edge).

**Done when:** play_native boots to the title, navigates to level select, starts the chosen level —
each screen matching the original's.

**State (2026-07-05):** full-screen images decode byte-exact; menu logic pure (systems/menu);
nothing wired beyond the title splash. Do after L1 plays.
