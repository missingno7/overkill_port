# Campaign: AUDIO (tier: EVENT-exact)

**Scope.** The BEFF sound queue (the walk already writes the right events: 0x0B spawn, 0x0F hit,
0x19 explosion, 0x1A shot...) → a host mixer playing the right sound at the right event; music later.

**Done when:** the queued events audibly play; the mix may be modern (event/timing-exact tier).

**State (2026-07-05):** not started; the EVENT side already falls out of the recovered walk. Last.
