# Campaign: RENDER (tier: PIXEL-exact, mechanism-flexible)

**Scope.** The composed frame: starfield plate + object sprites + tiles + HUD chrome/counters.

**Done when:** play_native's frame contains the HUD layer (composers exist, unwired) + the anim!=0 /
OR-inverted sprite variants; pixel-exact vs the VM present page on the capture corpus.

**State (2026-07-05):** playfield + sprites byte-exact on captures; HUD + sprite variants unwired.
Low urgency; do after Combat.
