__pycache__
backup
.pytest_cache
__bigtest.txt
assets

# Local assistant/tool state
.claude/

# Scratch traces, dumps, and rendered probes
trace_start.txt
checkpoint_tail.txt
seg1010_*.bin
tmp_*.bin
tmp_*.png
ega_first_present.png
ega_first_present.ppm

# Interactive/viewer smoke-test logs
artifacts/play_*_smoke_stdout.txt
artifacts/play_*_smoke_stderr.txt

# Generated visual/debug capture folders
artifacts/drive_menu_frames/
artifacts/drive_menu_frames2/
artifacts/headless_ega_frames/
artifacts/headless_ega_press/
artifacts/snapshot_play_*/
artifacts/verify_*/
artifacts/gap_snapshots/
artifacts/hard_tail_snaps/
# Generated artifact families; promote only selected evidence/test oracles.
artifacts/tmp_*/
artifacts/play_*/
artifacts/frame_verify/
artifacts/evidence/frame_verify/
artifacts/evidence/snapshot_play_previews/
# Crash/divergence repro captures (regenerated on demand; promote with -f if a
# specific one ever becomes a test fixture).
artifacts/repros/
# Python/tool caches
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Hook coverage warm-start cache: a play.py runtime byproduct with a per-run
# "updated" timestamp (so it churns every run); no test depends on it and it
# self-rebuilds. Untracked to keep the tree clean for unattended /goal runs.
artifacts/hook_coverage_cache.json
# Stray verify/run logs dropped in artifacts/ root (byproducts, not fixtures).
artifacts/*.log

# Python package/build outputs
build/
dist/
*.egg-info/
frame.png

# JetBrains IDE config
.idea/

overkill_level*_map.png
overkill_terrain_*.png
overkill_frame_*.png
overkill_native_montage.png
overkill_playfield.png
overkill_flythrough.gif
artifacts/shadow_cache/
artifacts/boot_entry_snapshot/
artifacts/boot_1010_entry/
artifacts/boot_init_snapshot/
artifacts/boot_lifts/
artifacts/*.wav
artifacts/frontend_intro_snapshot/
# the overlay-menu wedge image (1F8F:09A2), for disassembling the menu waits with
# scripts/lindis.py -- large game-derived binary, recreate rather than track
artifacts/overlay_menu_wedge_snapshot/
artifacts/intro_frame_dump/
artifacts/idle_boot_capture*/

# DOS_RE 2.0 pipeline output -- all regeneratable from the binary + IR + facts
# (scripts/probe_vmless_cpuless.py). Disposable per dos_re_2.0.md Principle 6.
artifacts/recovery_ir.json
artifacts/vmless_emit/
artifacts/cpuless_recovered/
artifacts/cpuless_adapters/
artifacts/cpuless_census.json
artifacts/cpuless_promote_census.json
artifacts/recovery_ir_closed.json
artifacts/lift_census_entries_closed.txt
artifacts/cpuless_promote_census_closed.json
artifacts/cpuless_closure.json
artifacts/cpuless_closure_gameplay.json
artifacts/liftverify_slice.txt
artifacts/liftverify_emit/
artifacts/indirect_sites.json
artifacts/cpuless_promote_census_dyn.json
# overkill/cpuless_recovered/ is NO LONGER ignored -- it is COMMITTED runtime source:
# the generated CPUless corpus play_cpuless imports as the game's default implementations
# (mirrors lemmings/recovered/; see docs/overkill/campaigns/cpuless_app.md). Regenerate with
# scripts/probe_vmless_cpuless.py; the CPU-ABI *adapters* stay disposable (verification-only).
overkill/cpuless_adapters/
artifacts/verify_*.txt
artifacts/cpuless_census_view.json
artifacts/viewA_differential.txt

# M3b ABI-recovered pipeline output (regeneratable: tools/contract_census.py + abi_promote.py)
artifacts/abi/

# verification ledgers (regeneratable: verify_cpuless.py --ledger)
artifacts/verify_ledger_*.json
