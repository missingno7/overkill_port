__pycache__
backup
.pytest_cache
__bigtest.txt
assets

# Local assistant/tool state
.claude/

# Scratch traces, dumps, and rendered probes
trace_start.txt
checkpoint_tail.txt
seg1010_*.bin
tmp_*.bin
tmp_*.png
ega_first_present.png
ega_first_present.ppm

# Interactive/viewer smoke-test logs
artifacts/play_*_smoke_stdout.txt
artifacts/play_*_smoke_stderr.txt

# Generated visual/debug capture folders
artifacts/drive_menu_frames/
artifacts/drive_menu_frames2/
artifacts/headless_ega_frames/
artifacts/headless_ega_press/
artifacts/snapshot_play_*/
artifacts/verify_*/
# Generated artifact families; promote only selected evidence/test oracles.
artifacts/tmp_*/
artifacts/play_*/
artifacts/frame_verify/
artifacts/evidence/frame_verify/
artifacts/evidence/snapshot_play_previews/
# Crash/divergence repro captures (regenerated on demand; promote with -f if a
# specific one ever becomes a test fixture).
artifacts/repros/
# Python/tool caches
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Hook coverage warm-start cache: a play.py runtime byproduct with a per-run
# "updated" timestamp (so it churns every run); no test depends on it and it
# self-rebuilds. Untracked to keep the tree clean for unattended /goal runs.
artifacts/hook_coverage_cache.json
# Stray verify/run logs dropped in artifacts/ root (byproducts, not fixtures).
artifacts/*.log

# Python package/build outputs
build/
dist/
*.egg-info/
frame.png

# JetBrains IDE config
.idea/

overkill_level*_map.png
overkill_terrain_*.png
overkill_frame_*.png
overkill_native_montage.png
overkill_playfield.png
overkill_flythrough.gif
artifacts/shadow_cache/
