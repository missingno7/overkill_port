"""Pure recovered domain records.

This package is intentionally free of ``cpu``, ``mem``, segment registers, and
DOS addresses as execution dependencies.  It contains copied source-like records
that can later be reused by a native source-port runtime.
"""
