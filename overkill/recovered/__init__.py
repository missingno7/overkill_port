"""Verifier-backed recovered source layer for OVERKILL.

Modules under ``overkill.recovered`` are intentionally conservative: they are
source-like names for structures and primitives that multiple lifted hooks have
already constrained.  They must stay close to the original DOS memory layout and
CPU side effects; speculative gameplay models belong in docs, not here.
"""
